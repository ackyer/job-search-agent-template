# JOBS — APPLIED (ARCHIVE)

> History of jobs already applied to (by the agent or by hand). The agent moves entries here
> from `JOBS.md` when `Applied:` contains `✅` or `Applied manually:` has content. See the
> archive rule in `PROJECT_INSTRUCTIONS.md` §6.

*(Nothing applied to yet.)*
